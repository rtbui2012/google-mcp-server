/**
 * Tool definitions for the MCP Google Search Server
 *
 */
import { googleSearchContent } from "./googleSearchContent";
import { googleSearchImages } from "./googleSearchImages";

export const tools = {
  googleSearchContent,
  googleSearchImages,
};